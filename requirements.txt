# Runtime dependencies for Rivet. Kept intentionally small — no ML frameworks,
# no system-level dependencies (Graphviz, Tesseract) — the engine is pure
# procedural geometry + constraint scoring.
networkx>=3.2
numpy>=1.26.0
Pillow>=10.0.0,<11.0.0
svgwrite>=1.4.3
ezdxf>=1.1.0
Flask>=3.0.0
